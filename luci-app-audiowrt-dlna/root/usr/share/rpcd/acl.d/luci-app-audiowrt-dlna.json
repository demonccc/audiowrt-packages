{
  "luci-app-audiowrt-renderer": {
    "description": "Grant access to AudioWRT renderer and discovery configuration and status",
    "read": {
      "uci": [ "audiowrt-dlna" ],
      "file": { "/usr/libexec/audiowrt-renderer": [ "exec" ] }
    },
    "write": {
      "uci": [ "audiowrt-dlna" ],
      "file": { "/usr/libexec/audiowrt-renderer": [ "exec" ] }
    }
  }
}
